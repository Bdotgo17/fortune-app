Frontend — Fortune App

Quick start

1. cd frontend
2. npm install
3. npm run dev

The app will open at http://localhost:5173 by default.

It expects the backend running at http://localhost:4000 for the API endpoints.
